name: 05 -1 - Event Filters & Activity Types
on:
    pull_request:
        types:
            - opened
            - synchronize
        branches:
            - main
            


jobs:
    echo_job:
        runs-on: ubuntu_latest
        steps:
            - run: echo "Running whenever a PR is opened or synchronised and base branch is main"

